# Legacy Backend Files

Esta pasta guarda arquivos antigos que não participam do backend atual.

Conteúdo:

- `models.js`: modelo legado preparado para MongoDB, preservado como referência.

O backend em uso hoje está organizado em:

- `app.js`
- `data/`
- `routes/`
